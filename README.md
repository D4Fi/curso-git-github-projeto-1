# git alteraçao feita no terminal com o git via api do github

## Como usar o git via api do github
## Como usar o git via api do github
## Como usar o git via api do github
## Como usar o git via api do github

Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
Para alterar o git no terminal, basta digitar no terminal o comando git.
